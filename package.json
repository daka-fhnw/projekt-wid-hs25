{
  "name": "projekt-wid-hs25",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix ./frontend",
    "postinstall": "npm install --prefix ./frontend"
  }
}
